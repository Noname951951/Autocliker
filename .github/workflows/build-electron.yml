name: Build Electron App

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: cd KeyControl && npm install

      - name: Build web app
        run: cd KeyControl && npm run build

      - name: Install Electron Builder
        run: cd KeyControl && npm install electron-builder --save-dev

      - name: Build Electron app
        run: cd KeyControl && npx electron-builder --win --x64

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: KeyControl-Windows
          path: KeyControlee/dist/*.exe

